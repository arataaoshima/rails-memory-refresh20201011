<h1 class="text-center top-message-wrapper">ブログカテゴリー管理</h1>

<div class="container">
<table class="table">
  <thead>
    <tr>
      <th>カテゴリー名</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blog_categories.each do |blog_category| %>
      <tr>
        <td><%= blog_category.name %></td>
        <td><%= link_to 'Show', blog_category %></td>
        <td><%= link_to 'Edit', edit_blog_category_path(blog_category) %></td>
        <td><%= link_to 'Destroy', blog_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新しくブログのカテゴリーを作る', new_blog_category_path, class:"btn btn-outline-success" %>
</div>
