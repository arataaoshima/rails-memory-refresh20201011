<p id="notice"><%= notice %></p>

<h1>Lecture Users</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Lecture</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lecture_users.each do |lecture_user| %>
      <tr>
        <td><%= lecture_user.user_id %></td>
        <td><%= lecture_user.lecture_id %></td>
        <td><%= link_to 'Show', lecture_user %></td>
        <td><%= link_to 'Edit', edit_lecture_user_path(lecture_user) %></td>
        <td><%= link_to 'Destroy', lecture_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lecture User', new_lecture_user_path %>
